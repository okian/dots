;; -*- no-byte-compile: t; -*-
;;; packages.el
;; Declare extra packages here, then run `doom sync`. Examples:
;; (package! some-package)
;; (package! another :recipe (:host github :repo "user/repo"))

;; Catppuccin theme — keeps Emacs in step with `dots theme` (the nord/
;; tokyo-night/gruvbox slugs map to Doom's built-in doom-themes). WezTerm,
;; nushell, Neovim and television all follow the same active palette.
(package! catppuccin-theme)
