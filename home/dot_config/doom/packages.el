;; -*- no-byte-compile: t; -*-
;;; packages.el
;; Declare extra packages here, then run `doom sync`. Examples:
;; (package! some-package)
;; (package! another :recipe (:host github :repo "user/repo"))

;; Catppuccin theme — keeps Emacs in step with the rest of the setup
;; (WezTerm / nushell / FZF / tmux / Neovim all run Catppuccin Mocha).
(package! catppuccin-theme)
