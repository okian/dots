# ~/.ssh/config — managed by chezmoi. (Private keys are encrypted/gated separately.)

Host *
    ServerAliveInterval 60
    ServerAliveCountMax 3
    IdentitiesOnly yes
    AddKeysToAgent yes
{{- if .isMac }}
    UseKeychain yes
{{- end }}
